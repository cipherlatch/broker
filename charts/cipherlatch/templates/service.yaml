apiVersion: v1
kind: Service
metadata:
  name: {{ include "cipherlatch.fullname" . }}
  labels:
    {{- include "cipherlatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "cipherlatch.selectorLabels" . | nindent 4 }}
